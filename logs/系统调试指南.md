# 🔧 在线大学生选课系统 - 完整调试指南

## 📊 系统状态检查

### ✅ 当前已安装的依赖
```
✅ Python 3.9.12
✅ FastAPI 0.115.12  
✅ Uvicorn 0.34.3
✅ MySQL Connector 9.3.0
✅ Pydantic 2.11.5
```

### ❌ 仍需安装的依赖
```
❌ pydantic-settings  - 配置管理
❌ python-decouple    - 环境变量处理
❌ python-jose        - JWT令牌处理
❌ passlib[bcrypt]    - 密码加密
❌ python-multipart   - 文件上传支持
❌ aiofiles           - 异步文件操作
❌ email-validator    - 邮箱验证
❌ python-dateutil    - 日期时间处理
```

## 🚀 当前启动状态

### 简化版服务器 ✅ 运行中
- 📍 **服务地址**: http://localhost:8000
- 📚 **API文档**: http://localhost:8000/docs  
- ❤️ **健康检查**: http://localhost:8000/health
- 🧪 **测试端点**: http://localhost:8000/test

### 启动命令
```powershell
# 当前使用（简化版）
python main_simple.py

# 完整版本（需要安装所有依赖）
python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

---

## 🔍 组件调试指南

### 1. 🖥️ 后端API调试

#### 检查服务状态
```powershell
# 检查服务是否启动
curl http://localhost:8000/health
# 或在浏览器访问: http://localhost:8000/health
```

#### API文档调试
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc
- **OpenAPI Schema**: http://localhost:8000/api/v1/openapi.json

#### 日志调试
```powershell
# 查看实时日志
python main_simple.py
# 日志级别在 app/core/config_simple.py 中设置
```

#### 数据库连接调试
```powershell
# 测试数据库连接
python -c "
from app.db.mysql_client import mysql_client
success, results, error = mysql_client.execute_raw_sql('SELECT 1 as test;')
print('数据库连接:', '成功' if success else f'失败: {error}')
"
```

### 2. 🗄️ 数据库调试

#### 连接测试
```powershell
# 使用Python测试
python -c "
import mysql.connector
try:
    conn = mysql.connector.connect(
        host='localhost',
        port=3306,
        user='root',
        password='',
        database='student_course_system'
    )
    print('✅ 数据库连接成功')
    conn.close()
except Exception as e:
    print(f'❌ 数据库连接失败: {e}')
"
```

#### 表结构检查
```sql
-- 检查数据库和表
SHOW DATABASES;
USE student_course_system;
SHOW TABLES;

-- 检查关键表结构
DESCRIBE students;
DESCRIBE courses;
DESCRIBE enrollments;
```

#### 数据完整性检查
```sql
-- 检查数据统计
SELECT 'students' as table_name, COUNT(*) as count FROM students
UNION ALL
SELECT 'courses', COUNT(*) FROM courses  
UNION ALL
SELECT 'departments', COUNT(*) FROM departments
UNION ALL
SELECT 'enrollments', COUNT(*) FROM enrollments;
```

### 3. 🎨 前端调试

#### 环境检查
```powershell
# 切换到前端目录
cd frontend

# 检查Node.js和npm
node --version
npm --version

# 检查依赖安装
npm list --depth=0
```

#### 启动前端开发服务器
```powershell
cd frontend

# 安装依赖（如果需要）
npm install

# 启动开发服务器
npm run dev

# 访问地址: http://localhost:5173
```

#### 前端构建测试
```powershell
cd frontend

# 构建生产版本
npm run build

# 预览构建结果
npm run preview
```

---

## 🛠️ 完整系统调试

### 系统启动顺序

#### 1. 📊 环境检查
```powershell
# 检查Python环境
python --version
pip list | findstr -i "fastapi uvicorn mysql"

# 检查数据库服务
# 确保MySQL服务正在运行
```

#### 2. 🗄️ 数据库准备
```powershell
# 创建数据库（如果不存在）
mysql -u root -p -e "CREATE DATABASE IF NOT EXISTS student_course_system;"

# 导入初始数据（如果需要）
# mysql -u root -p student_course_system < database/init.sql
```

#### 3. 🖥️ 后端启动
```powershell
cd backend

# 方案A: 简化版（当前可用）
python main_simple.py

# 方案B: 完整版（需要所有依赖）
python -m uvicorn main:app --reload
```

#### 4. 🎨 前端启动
```powershell
# 新建终端窗口
cd frontend
npm run dev
```

### 端口配置

| 服务 | 默认端口 | 访问地址 |
|------|----------|----------|
| 后端API | 8000 | http://localhost:8000 |
| 前端开发 | 5173 | http://localhost:5173 |
| MySQL | 3306 | localhost:3306 |

---

## 🚨 常见问题排查

### 1. 依赖安装问题

#### 问题: `No module named 'xxx'`
```powershell
# 解决方案1: 检查虚拟环境
pip list | findstr xxx

# 解决方案2: 重新安装
pip install xxx

# 解决方案3: 使用国内镜像
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple/ xxx
```

#### 问题: 网络代理错误
```powershell
# 清除代理设置
$env:HTTP_PROXY = $null
$env:HTTPS_PROXY = $null

# 重置pip配置
pip config unset global.proxy
pip config unset global.index-url
```

### 2. 数据库连接问题

#### 问题: `Access denied for user`
- 检查MySQL服务是否启动
- 确认用户名密码正确
- 检查数据库是否存在

#### 问题: `Can't connect to MySQL server`
- 确认MySQL服务正在运行
- 检查端口3306是否开放
- 验证防火墙设置

### 3. 端口占用问题

#### 检查端口占用
```powershell
# 检查8000端口
netstat -ano | findstr :8000

# 杀死占用进程
taskkill /PID xxxx /F
```

### 4. 跨域问题

#### 前后端通信错误
- 检查CORS配置
- 确认前端请求地址正确
- 验证API端点路径

---

## 📈 性能调试

### 1. API性能监控

#### 响应时间检查
```powershell
# 使用curl测试响应时间
curl -w "@curl-format.txt" -o /dev/null -s http://localhost:8000/health

# curl-format.txt 内容:
#      time_namelookup:  %{time_namelookup}\n
#      time_connect:     %{time_connect}\n
#      time_total:       %{time_total}\n
```

#### 内存使用监控
```powershell
# 检查Python进程内存使用
tasklist | findstr python.exe
```

### 2. 数据库性能

#### 慢查询检查
```sql
-- 开启慢查询日志
SET GLOBAL slow_query_log = 'ON';
SET GLOBAL long_query_time = 1;

-- 查看慢查询
SHOW VARIABLES LIKE 'slow_query%';
```

#### 连接数监控
```sql
-- 查看当前连接数
SHOW STATUS LIKE 'Threads_connected';
SHOW STATUS LIKE 'Max_used_connections';
```

---

## 🧪 测试策略

### 1. API测试

#### 健康检查测试
```powershell
# 基础连通性
curl http://localhost:8000/

# 健康状态
curl http://localhost:8000/health

# API文档可访问性
curl http://localhost:8000/docs
```

#### 功能API测试
```powershell
# 认证测试（需要完整版）
curl -X POST "http://localhost:8000/api/v1/auth/login" \
     -H "Content-Type: application/json" \
     -d '{"username": "test", "password": "test123"}'
```

### 2. 集成测试

#### 前后端联调
1. 启动后端服务 (端口8000)
2. 启动前端服务 (端口5173)  
3. 在前端测试API调用
4. 检查浏览器网络面板

#### 数据库集成测试
1. 测试数据库连接
2. 验证CRUD操作
3. 检查事务完整性

---

## 📝 开发建议

### 1. 开发环境配置

#### 推荐的开发工具
- **IDE**: VS Code / PyCharm
- **API测试**: Postman / Insomnia
- **数据库**: MySQL Workbench / phpMyAdmin
- **版本控制**: Git

#### 环境变量配置
```powershell
# 创建 .env 文件
cat > .env << EOF
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=student_course_system
SECRET_KEY=your-secret-key-here
DEBUG=true
EOF
```

### 2. 代码调试

#### Python调试
```python
# 在代码中添加断点
import pdb; pdb.set_trace()

# 或者使用logging
import logging
logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)
logger.debug("调试信息")
```

#### 前端调试
```javascript
// 在浏览器控制台
console.log('调试信息');
console.error('错误信息');

// Vue Devtools
// 安装浏览器插件进行组件调试
```

---

## 🆘 获取帮助

### 日志文件位置
- 应用日志: 控制台输出
- 数据库日志: MySQL错误日志
- 前端日志: 浏览器控制台

### 常用命令速查
```powershell
# 后端相关
python main_simple.py              # 启动简化版后端
python -m uvicorn main:app --reload # 启动完整版后端
pip list                           # 查看已安装包
python -c "import xxx"             # 测试包导入

# 前端相关  
npm run dev                        # 启动开发服务器
npm run build                      # 构建生产版本
npm install                        # 安装依赖

# 数据库相关
mysql -u root -p                   # 连接数据库
SHOW DATABASES;                    # 显示数据库列表
USE student_course_system;         # 切换数据库
```

### 技术支持
1. 📖 查看项目文档
2. 🔍 搜索错误信息
3. 📝 记录详细的错误日志
4. 🤝 寻求技术支持

---

## 📋 检查清单

### 启动前检查
- [ ] Python环境正常
- [ ] 必要依赖已安装
- [ ] MySQL服务运行中
- [ ] 端口8000未被占用
- [ ] 数据库连接正常

### 功能检查
- [ ] 后端API响应正常
- [ ] 前端页面加载正常
- [ ] 数据库读写正常
- [ ] 跨域配置正确
- [ ] 日志输出正常

### 性能检查
- [ ] API响应时间 < 2秒
- [ ] 内存使用合理
- [ ] 数据库查询优化
- [ ] 前端加载速度正常

---

## 🎯 下一步计划

1. **解决依赖问题**: 安装所有缺失的Python包
2. **完整功能测试**: 测试所有40个API端点
3. **前端集成**: 连接前端Vue.js应用  
4. **数据库优化**: 性能调优和索引优化
5. **部署准备**: 配置生产环境 